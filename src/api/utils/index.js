// Placeholder cho các helper API chung (tương tự utils trong SWP-391)
// Hiện tại toàn bộ xử lý interceptor, token, error đang được đặt trong api/config/client.js.
// Nếu sau này cần chuẩn hoá thêm (ví dụ: chuẩn hoá lỗi, logging), có thể thêm hàm vào đây
// và sử dụng trong các service mà không phải thay đổi logic hiện tại.

export {};


